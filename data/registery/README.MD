# Dataset Registry

Tracks dataset sources, licenses, and integrity metadata.

Register a dataset:
python pipeline/register_dataset.py --dataset-id <id> --source <source> --origin <origin> --raw-path <path>
